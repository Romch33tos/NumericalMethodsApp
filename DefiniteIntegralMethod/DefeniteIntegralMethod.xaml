<Window x:Class="NumericalMethodsApp.DefiniteIntegralMethod.DefeniteIntegralMethod"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="Вычисление определенного интеграла" Height="720" Width="1000" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="White"
        x:Name="ThisWindow">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                             VerticalAlignment="Center" 
                                             Margin="12,8"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF21618C"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFEEEEEE"/>
                    <Setter Property="Foreground" Value="#FF888888"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFD0D7DE"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="8"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="4,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="4,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="ResultTextBoxStyle" TargetType="TextBox">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="0,0,0,-16">
        <Menu DockPanel.Dock="Top" Background="White">
            <MenuItem Header="Справка" Click="Help_Click" Background="White" BorderBrush="White"/>
        </Menu>

        <Border Background="White" Margin="15" CornerRadius="8" 
                BorderThickness="1" BorderBrush="#FFEAECEE" Height="640">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="220"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="1" Grid.ColumnSpan="3" Height="220" Margin="8" 
                        BorderThickness="1" BorderBrush="#FFEAECEE" 
                        Background="#FFF8F9FA" CornerRadius="6">
                    <oxy:PlotView x:Name="PlotViewControl" Background="Transparent" Margin="5,5,5,23"/>
                </Border>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="8">
                    <Label Content="Функция f(x):" FontWeight="Bold" FontSize="13"/>
                    <TextBox x:Name="FunctionTextBox" 
                            Width="476" 
                            ToolTip="Введите математическую функцию, например: x^2 + 2*x + 1"
                            Text="{Binding FunctionExpression, ElementName=ThisWindow}"/>
                </StackPanel>

                <GroupBox Grid.Row="3" Grid.Column="0" Header="Границы интегрирования" Margin="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Content="Начало (a):" Grid.Row="0" Grid.Column="0"/>
                        <TextBox x:Name="TextBoxA" Grid.Row="0" Grid.Column="1" 
                                ToolTip="Нижняя граница интегрирования"
                                Text="{Binding LowerBound, ElementName=ThisWindow}"/>

                        <Label Content="Конец (b):" Grid.Row="1" Grid.Column="0"/>
                        <TextBox x:Name="TextBoxB" Grid.Row="1" Grid.Column="1" 
                                ToolTip="Верхняя граница интегрирования"
                                Text="{Binding UpperBound, ElementName=ThisWindow}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="3" Grid.Column="1" Header="Точность вычислений" Margin="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Content="Значение ε:" Grid.Row="0" Grid.Column="0"/>
                        <TextBox x:Name="EpsilonTextBox" Grid.Row="0" Grid.Column="1" 
                                ToolTip="Точность вычисления (например: 0.001)" 
                                Text="{Binding Epsilon, ElementName=ThisWindow}"/>

                        <Label Content="Количество разбиений (n):" Grid.Row="1" Grid.Column="0"/>
                        <TextBox x:Name="PartitionsTextBox" Grid.Row="1" Grid.Column="1" 
                                ToolTip="Количество разбиений интервала"
                                Text="{Binding FixedPartitions, ElementName=ThisWindow}"
                                IsEnabled="{Binding UseFixedPartitions, ElementName=ThisWindow}"/>

                        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,8,0,0">
                            <RadioButton x:Name="AutoPartitionsRadio" Content="Автоматически" 
                                       IsChecked="{Binding AutoPartitions, ElementName=ThisWindow}"
                                       Margin="4,0" Width="148"/>
                            <RadioButton x:Name="FixedPartitionsRadio" Content="Ручной ввод" 
                                       IsChecked="{Binding UseFixedPartitions, ElementName=ThisWindow}"
                                       Margin="4,0" Width="98"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="3" Grid.Column="2" Header="Методы интегрирования" Margin="8">
                    <StackPanel>
                        <CheckBox Content="Левые прямоугольники" x:Name="LeftRectCheckBox" 
                                 Margin="2"
                                 IsChecked="{Binding IsLeftRectSelected, ElementName=ThisWindow}"/>
                        <CheckBox Content="Правые прямоугольники" x:Name="RightRectCheckBox" 
                                 Margin="2"
                                 IsChecked="{Binding IsRightRectSelected, ElementName=ThisWindow}"/>
                        <CheckBox Content="Средние прямоугольники" x:Name="MidRectCheckBox" 
                                 Margin="2"
                                 IsChecked="{Binding IsMidRectSelected, ElementName=ThisWindow}"/>
                        <CheckBox Content="Метод трапеций" x:Name="TrapezoidCheckBox" 
                                 Margin="2"
                                 IsChecked="{Binding IsTrapezoidSelected, ElementName=ThisWindow}"/>
                        <CheckBox Content="Метод Симпсона" x:Name="SimpsonCheckBox" 
                                 Margin="2"
                                 IsChecked="{Binding IsSimpsonSelected, ElementName=ThisWindow}"/>
                    </StackPanel>
                </GroupBox>

                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" 
                           HorizontalAlignment="Center" Margin="0,8,0,8">
                    <Button Content="Вычислить" Click="CalculateButton_Click" Width="136" 
                           Background="#FF27AE60" FontSize="13" Margin="8,0"/>
                    <Button Content="Очистить все" Click="ClearAllButton_Click" Width="135" 
                           Background="#FF3498DB" FontSize="13" Margin="8,0"/>
                </StackPanel>

                <GroupBox Grid.Row="5" Grid.ColumnSpan="3" Header="Результаты" Margin="0,10,16,10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <TextBox x:Name="ResultTextBox" 
                                Style="{StaticResource ResultTextBoxStyle}"
                                Text="{Binding ResultText, ElementName=ThisWindow}"
                                Height="60"
                                MinHeight="70"
                                MaxHeight="500"/>
                    </ScrollViewer>
                </GroupBox>
            </Grid>
        </Border>
    </DockPanel>
</Window>
