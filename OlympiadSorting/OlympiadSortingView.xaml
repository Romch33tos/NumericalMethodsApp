<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NumericalMethodsApp.OlympiadSorting"
        xmlns:oxy="http://oxyplot.org/wpf" 
        x:Class="NumericalMethodsApp.OlympiadSorting.OlympiadSortingView"
        mc:Ignorable="d"
        Title="Сортировка массивов" Height="900" Width="1300"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="White"
        x:Name="ThisWindow">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                             VerticalAlignment="Center" 
                                             Margin="12,8"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF21618C"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFEEEEEE"/>
                    <Setter Property="Foreground" Value="#FF888888"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFD0D7DE"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="8"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="12,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="ResultTextBoxStyle" TargetType="TextBox">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="0">
        <Menu DockPanel.Dock="Top" Background="White">
            <MenuItem Header="Справка" Click="Help_Click"/>
        </Menu>

        <Border Background="White" Margin="15" CornerRadius="8" 
                BorderThickness="1" BorderBrush="#FFEAECEE" Height="847">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <GroupBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Header="Массивы" Margin="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid x:Name="OriginalDataGrid" Grid.Column="0" 
                                 Height="160" Margin="8,4,4,4" SelectionChanged="DataGrid_SelectionChanged"/>

                        <DataGrid x:Name="SortedDataGrid" Grid.Column="1" 
                                 Height="160" Margin="4,4,8,4"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Header="Сравнение алгоритмов сортировки (время выполнения)" Margin="8">
                    <Border BorderThickness="1" BorderBrush="#FFEAECEE" 
                           Background="#FFF8F9FA" CornerRadius="6">
                        <oxy:PlotView x:Name="PerformanceChart" Background="Transparent" Margin="5"/>
                    </Border>
                </GroupBox>

                <GroupBox Grid.Row="1" Grid.RowSpan="2" Grid.Column="2" Header="Результаты выполнения" Margin="8">
                    <DataGrid x:Name="ResultsDataGrid" Height="332" Margin="8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Алгоритм" Width="*" Binding="{Binding AlgorithmName}"/>
                            <DataGridTextColumn Header="Время (мс)" Width="80" Binding="{Binding TimeMs}"/>
                            <DataGridTextColumn Header="Итерации" Width="70" Binding="{Binding Iterations}"/>
                            <DataGridTextColumn Header="Элементы" Width="70" Binding="{Binding ElementCount}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <GroupBox Grid.Row="3" Grid.Column="0" Header="Генерация данных" Margin="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Content="Размерность:" Grid.Row="0" Grid.Column="0"/>
                        <TextBox x:Name="RowsTextBox" Grid.Column="1" Text="5" 
                                ToolTip="Количество строк" Margin="8,4,65,0"/>

                        <Label Content="Диапазон:" Grid.Row="1" Grid.Column="0"/>
                        <TextBox x:Name="MinValueTextBox" Grid.Row="1" Grid.Column="1" Text="0" 
                                ToolTip="Минимальное значение"/>
                        <Label Content="-" Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center"/>
                        <TextBox x:Name="MaxValueTextBox" Grid.Row="1" Grid.Column="3" Text="100" 
                                ToolTip="Максимальное значение" Margin="8,4,0,4"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="3" Grid.Column="1" Header="Импорт данных" Margin="8">
                    <StackPanel Orientation="Vertical">
                        <Button Content="Случайная генерация" Click="RandomGenerate_Click" 
                               Margin="4" Background="#FF27AE60"/>
                        <Button Content="Импорт из CSV" Click="ImportCsv_Click" 
                               Margin="4"/>
                        <Button Content="Импорт из Google Таблиц" Click="ImportGoogle_Click" 
                               Margin="4"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Grid.Row="3" Grid.Column="2" Header="Алгоритмы сортировки" Margin="8">
                    <StackPanel Margin="8">
                        <CheckBox x:Name="BubbleSortCheckBox" Content="Пузырьковая" Checked="CheckBox_Checked"/>
                        <CheckBox x:Name="InsertionSortCheckBox" Content="Вставками" Checked="CheckBox_Checked"/>
                        <CheckBox x:Name="ShakerSortCheckBox" Content="Шейкерная" Checked="CheckBox_Checked"/>
                        <CheckBox x:Name="QuickSortCheckBox" Content="Быстрая" Checked="CheckBox_Checked"/>
                        <CheckBox x:Name="BogosortCheckBox" Content="Болотная" Checked="CheckBox_Checked"/>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </Border>
    </DockPanel>
</Window>
