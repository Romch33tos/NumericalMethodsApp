<Window x:Class="NumericalMethodsApp.LeastSquaresMethod.GoogleSheetsImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт из Google Таблиц" Height="200" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF0F0F0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Импорт данных из Google Таблиц" 
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Введите ссылку на Google Таблицу:" 
                   Margin="0,0,0,5"/>

        <TextBox x:Name="UrlTextBox" Grid.Row="2" Height="25" 
                 Margin="0,0,0,10" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="CancelButton_Click" 
                    Width="80" Height="25" Margin="0,0,10,0"/>
            <Button Content="Импорт" Click="ImportButton_Click" 
                    Width="80" Height="25" Background="#FF4A6CD4" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
